import tkinter as tk

root = tk.Tk()

shopping_img = tk.PhotoImage(
    file="assets/shopping.png"
)

img_label = tk.Label(
    root,
    image=shopping_img
)

img_label.pack()

root.mainloop()